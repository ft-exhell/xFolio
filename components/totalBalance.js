export default function TotalBalance({ addresses }) {
    if (addresses) {
        addresses
    }

    return(
        <div>
            <h2 className="font-medium leading-tight text-3xl">Your total balance is</h2>
        </div>
    )
}